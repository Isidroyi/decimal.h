#include "s21_decimal.h"

int s21_is_greater(s21_decimal val1, s21_decimal val2) {
  /*Функция s21_is_greater сравнивает два значения типа s21_decimal и возвращает
   * 1, если первое значение больше второго, или 0, если нет.*/
  return s21_is_less(val2, val1);
  /*Возвращается результат функции s21_is_less, который будет 1, если первое
   * значение больше второго, или 0, если нет.*/
}